import React from 'react';
import ClaseItem from '../ClaseItem';

// eslint-disable-next-line import/prefer-default-export
export function renderClase(clase) {
  return <ClaseItem clase={clase} />;
}
