const RESOURCE = 'CLASSES_';

export const SET_CLASSES = `${RESOURCE}SET`;
export const CLEAR_CLASSES = `${RESOURCE}CLEAR`;
export const ADD_CLASS = `${RESOURCE}ADD`;
export const REMOVE_CLASS = `${RESOURCE}REMOVE`;
export const MODIFY_CLASS = `${RESOURCE}MODIFY`;
